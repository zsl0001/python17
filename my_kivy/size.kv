<SizeFloat>:

    canvas:
		Color:
			rgba: [1, 1, 1, 1]
		Rectangle:
			size: self.size
			pos: self.pos

    Button:
        text: 'Hello'
        size_hint: .2, .15
        pos_hint: {'x': 0.2, 'y': .6}

    Button:
        text: 'Size'
        size_hint: .2, .15
        pos_hint: {'right': 0.8, 'top': .4}
